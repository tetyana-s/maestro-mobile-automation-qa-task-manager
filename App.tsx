import { StatusBar } from 'expo-status-bar';
import { useMemo, useState } from 'react';
import { KeyboardAvoidingView, Platform, Pressable, SafeAreaView, ScrollView, StyleSheet, Switch, Text, TextInput, View } from 'react-native';

type Priority = 'Low' | 'Medium' | 'High';
type TaskStatus = 'Pending' | 'In Progress' | 'Completed';
type Screen = 'login' | 'dashboard' | 'add' | 'details' | 'edit' | 'settings';
type Task = { id: number; title: string; description: string; priority: Priority; status: TaskStatus; dueDate: string };

const initialTasks: Task[] = [
  { id: 1, title: 'Verify login', description: 'Confirm valid and invalid credential paths.', priority: 'High', status: 'In Progress', dueDate: 'Sep 14, 2026' },
  { id: 2, title: 'Test checkout flow', description: 'Cover cart, payment, and confirmation states.', priority: 'High', status: 'Pending', dueDate: 'Sep 16, 2026' },
  { id: 3, title: 'Validate push notifications', description: 'Check permission prompts and delivery behavior.', priority: 'Medium', status: 'Completed', dueDate: 'Sep 12, 2026' },
  { id: 4, title: 'Regression test payment', description: 'Run the critical payment regression suite.', priority: 'Medium', status: 'Pending', dueDate: 'Sep 20, 2026' },
  { id: 5, title: 'Test offline mode', description: 'Verify graceful recovery without connectivity.', priority: 'Low', status: 'Pending', dueDate: 'Sep 22, 2026' },
];
const priorities: Priority[] = ['Low', 'Medium', 'High'];
const statuses: TaskStatus[] = ['Pending', 'In Progress', 'Completed'];
const lightPalette = { background: '#F8F5EF', muted: '#756F66' };
const darkPalette = { background: '#17221F', muted: '#AAB8B2' };

export default function App() {
  const [screen, setScreen] = useState<Screen>('login');
  const [tasks, setTasks] = useState(initialTasks);
  const [selectedTask, setSelectedTask] = useState<Task | null>(null);
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [rememberMe, setRememberMe] = useState(false);
  const [loginError, setLoginError] = useState('');
  const [search, setSearch] = useState('');
  const [filter, setFilter] = useState<TaskStatus | Priority | 'All'>('All');
  const [darkMode, setDarkMode] = useState(false);
  const [draft, setDraft] = useState<Omit<Task, 'id'>>({ title: '', description: '', priority: 'Medium', status: 'Pending', dueDate: '' });
  const [formError, setFormError] = useState('');
  const palette = darkMode ? darkPalette : lightPalette;
  const filteredTasks = useMemo(() => tasks.filter((task) => task.title.toLowerCase().includes(search.toLowerCase()) && (filter === 'All' || task.status === filter || task.priority === filter)), [filter, search, tasks]);
  const openTask = (task: Task) => { setSelectedTask(task); setScreen('details'); };
  const beginAdd = () => { setDraft({ title: '', description: '', priority: 'Medium', status: 'Pending', dueDate: '' }); setFormError(''); setScreen('add'); };
  const beginEdit = () => { if (selectedTask) { setDraft({ title: selectedTask.title, description: selectedTask.description, priority: selectedTask.priority, status: selectedTask.status, dueDate: selectedTask.dueDate }); setFormError(''); setScreen('edit'); } };
  const saveTask = () => {
    if (!draft.title.trim()) { setFormError('Task title is required.'); return; }
    if (screen === 'edit' && selectedTask) {
      const updated = { ...selectedTask, ...draft, title: draft.title.trim() };
      setTasks((current) => current.map((task) => task.id === selectedTask.id ? updated : task)); setSelectedTask(updated); setScreen('details');
    } else {
      const created = { ...draft, id: Math.max(...tasks.map((task) => task.id), 0) + 1, title: draft.title.trim() };
      setTasks((current) => [created, ...current]); setSelectedTask(created); setScreen('details');
    }
  };
  const login = () => {
    if (!email.trim()) { setLoginError('Email is required.'); return; }
    if (!email.includes('@')) { setLoginError('Enter a valid email address.'); return; }
    if (!password) { setLoginError('Password is required.'); return; }
    if (email.toLowerCase() !== 'qa@example.com' || password !== 'Password123') { setLoginError('Invalid email or password.'); return; }
    setLoginError(''); setScreen('dashboard');
  };
  const logout = () => { setEmail(''); setPassword(''); setLoginError(''); setScreen('login'); };
  const completeTask = () => { if (!selectedTask) return; const updated = { ...selectedTask, status: 'Completed' as TaskStatus }; setTasks((current) => current.map((task) => task.id === updated.id ? updated : task)); setSelectedTask(updated); };
  const deleteTask = () => { if (!selectedTask) return; setTasks((current) => current.filter((task) => task.id !== selectedTask.id)); setSelectedTask(null); setScreen('dashboard'); };

  if (screen === 'login') return <LoginScreen email={email} password={password} rememberMe={rememberMe} error={loginError} setEmail={setEmail} setPassword={setPassword} setRememberMe={setRememberMe} onLogin={login} palette={palette} />;
  return <SafeAreaView style={[styles.safeArea, { backgroundColor: palette.background }]}><StatusBar style={darkMode ? 'light' : 'dark'} />
    {screen === 'dashboard' && <DashboardScreen tasks={tasks} filteredTasks={filteredTasks} search={search} filter={filter} setSearch={setSearch} setFilter={setFilter} onTask={openTask} onAdd={beginAdd} onSettings={() => setScreen('settings')} onLogout={logout} />}
    {(screen === 'add' || screen === 'edit') && <TaskFormScreen mode={screen} draft={draft} error={formError} setDraft={setDraft} onSave={saveTask} onCancel={() => setScreen(screen === 'edit' ? 'details' : 'dashboard')} />}
    {screen === 'details' && selectedTask && <DetailsScreen task={selectedTask} onBack={() => setScreen('dashboard')} onEdit={beginEdit} onComplete={completeTask} onDelete={deleteTask} />}
    {screen === 'settings' && <SettingsScreen darkMode={darkMode} setDarkMode={setDarkMode} onBack={() => setScreen('dashboard')} onLogout={logout} />}
  </SafeAreaView>;
}

function LoginScreen({ email, password, rememberMe, error, setEmail, setPassword, setRememberMe, onLogin }: any) {
  return <SafeAreaView style={styles.safeArea}><StatusBar style="dark" /><KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : undefined} style={styles.center}><View style={styles.loginCard}><Text style={styles.eyebrow}>QA OPERATIONS</Text><Text style={styles.logo}>QA Task Manager</Text><Text style={styles.subtitle}>Plan, execute, and track your test work.</Text><Text style={styles.label}>Email</Text><TextInput testID="login-email" accessibilityLabel="login-email" style={styles.input} value={email} onChangeText={setEmail} placeholder="qa@example.com" placeholderTextColor="#9A9A9A" autoCapitalize="none" keyboardType="email-address" /><Text style={styles.label}>Password</Text><TextInput testID="login-password" accessibilityLabel="login-password" style={styles.input} value={password} onChangeText={setPassword} placeholder="Enter password" placeholderTextColor="#9A9A9A" secureTextEntry /><Pressable testID="remember-me" accessibilityRole="checkbox" accessibilityState={{ checked: rememberMe }} style={styles.rememberRow} onPress={() => setRememberMe(!rememberMe)}><View style={[styles.checkbox, rememberMe && styles.checkboxChecked]}>{rememberMe && <Text style={styles.checkmark}>✓</Text>}</View><Text style={styles.body}>Remember me</Text></Pressable>{error ? <Text testID="login-error" accessibilityLabel="login-error" style={styles.error}>{error}</Text> : null}<Pressable testID="login-button" accessibilityLabel="login-button" style={styles.primaryButton} onPress={onLogin}><Text style={styles.primaryButtonText}>Log in</Text></Pressable><Text style={styles.demoHint}>Demo: qa@example.com / Password123</Text></View></KeyboardAvoidingView></SafeAreaView>;
}

function DashboardScreen({ tasks, filteredTasks, search, filter, setSearch, setFilter, onTask, onAdd, onSettings, onLogout }: any) {
  const completed = tasks.filter((task: Task) => task.status === 'Completed').length;
  const pending = tasks.filter((task: Task) => task.status !== 'Completed').length;
  return <ScrollView testID="dashboard-screen" contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><View style={styles.header}><View><Text style={styles.eyebrow}>QA OPERATIONS</Text><Text style={styles.heading}>Good morning, QA</Text><Text style={styles.subtitle}>Here is your test execution overview.</Text></View><Pressable testID="settings-button" accessibilityLabel="settings-button" style={styles.iconButton} onPress={onSettings}><Text style={styles.iconText}>⚙</Text></Pressable></View><View style={styles.statsRow}><Stat label="Total tasks" value={tasks.length} /><Stat label="Completed" value={completed} accent /><Stat label="Pending" value={pending} /></View><View style={styles.sectionHeader}><Text style={styles.sectionTitle}>Test tasks</Text><Pressable testID="add-task-button" accessibilityLabel="add-task-button" style={styles.addButton} onPress={onAdd}><Text style={styles.addButtonText}>+ Add task</Text></Pressable></View><TextInput testID="task-search" accessibilityLabel="task-search" style={styles.search} value={search} onChangeText={setSearch} placeholder="Search test tasks" placeholderTextColor="#9A9A9A"/><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.filters}>{(['All', ...statuses, ...priorities] as const).map((value) => <Pressable key={value} testID={`filter-${value.toLowerCase().replace(' ', '-')}`} onPress={() => setFilter(value)} style={[styles.filterChip, filter === value && styles.filterActive]}><Text style={[styles.filterText, filter === value && styles.filterTextActive]}>{value}</Text></Pressable>)}<Pressable testID="clear-filters" accessibilityLabel="clear-filters" onPress={() => { setFilter('All'); setSearch(''); }} style={styles.filterChip}><Text style={styles.filterText}>Clear</Text></Pressable></ScrollView>{filteredTasks.map((task: Task) => <TaskRow key={task.id} task={task} onPress={() => onTask(task)} />)}{filteredTasks.length === 0 && <Text style={styles.empty}>No matching test tasks.</Text>}<Pressable testID="logout-button" accessibilityLabel="logout-button" style={styles.logoutButton} onPress={onLogout}><Text style={styles.logoutText}>Log out</Text></Pressable></ScrollView>;
}
function Stat({ label, value, accent = false }: { label: string; value: number; accent?: boolean }) { return <View style={[styles.stat, accent && styles.statAccent]}><Text style={styles.statValue}>{value}</Text><Text style={styles.statLabel}>{label}</Text></View>; }
function TaskRow({ task, onPress }: { task: Task; onPress: () => void }) { return <Pressable testID={`task-item-${task.id}`} accessibilityLabel={`task-item-${task.id}`} style={styles.taskRow} onPress={onPress}><View style={styles.taskRowTop}><Text testID="task-title" style={styles.taskTitle}>{task.title}</Text><Text testID="task-priority" style={[styles.priority, priorityStyle[task.priority]]}>{task.priority}</Text></View><View style={styles.taskRowBottom}><Text testID="task-status" style={styles.statusText}>{task.status}</Text><Text style={styles.dueText}>{task.dueDate}</Text></View></Pressable>; }

function TaskFormScreen({ mode, draft, error, setDraft, onSave, onCancel }: any) { return <ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><View style={styles.formHeader}><Pressable testID="form-back-button" onPress={onCancel}><Text style={styles.backText}>‹ Back</Text></Pressable><Text style={styles.heading}>{mode === 'add' ? 'Add test task' : 'Edit test task'}</Text><Text style={styles.subtitle}>Keep the test scope clear and actionable.</Text></View><Text style={styles.label}>Task title</Text><TextInput testID="task-title-input" accessibilityLabel="task-title-input" style={styles.input} value={draft.title} onChangeText={(title) => setDraft({ ...draft, title })} placeholder="e.g. Verify login" placeholderTextColor="#9A9A9A"/><Text style={styles.label}>Description <Text style={styles.optional}>Optional</Text></Text><TextInput testID="task-description-input" accessibilityLabel="task-description-input" style={[styles.input, styles.textArea]} value={draft.description} onChangeText={(description) => setDraft({ ...draft, description })} placeholder="What should the tester verify?" placeholderTextColor="#9A9A9A" multiline /><Text style={styles.label}>Priority</Text><View style={styles.optionRow}>{priorities.map((priority) => <Pressable key={priority} testID={`priority-${priority.toLowerCase()}`} onPress={() => setDraft({ ...draft, priority })} style={[styles.option, draft.priority === priority && styles.optionSelected]}><Text style={draft.priority === priority ? styles.optionTextSelected : styles.optionText}>{priority}</Text></Pressable>)}</View>{mode === 'edit' && <><Text style={styles.label}>Status</Text><View style={styles.optionRow}>{statuses.map((status) => <Pressable key={status} testID={`status-${status.toLowerCase().replace(' ', '-')}`} onPress={() => setDraft({ ...draft, status })} style={[styles.option, draft.status === status && styles.optionSelected]}><Text style={draft.status === status ? styles.optionTextSelected : styles.optionText}>{status}</Text></Pressable>)}</View></>}<Text style={styles.label}>Due date</Text><TextInput testID="task-due-date-input" accessibilityLabel="task-due-date-input" style={styles.input} value={draft.dueDate} onChangeText={(dueDate) => setDraft({ ...draft, dueDate })} placeholder="e.g. Sep 30, 2026" placeholderTextColor="#9A9A9A"/>{error ? <Text testID="task-form-error" style={styles.error}>{error}</Text> : null}<View style={styles.buttonRow}><Pressable testID="cancel-task-button" style={styles.secondaryButton} onPress={onCancel}><Text style={styles.secondaryButtonText}>Cancel</Text></Pressable><Pressable testID="save-task-button" style={styles.primaryButtonSmall} onPress={onSave}><Text style={styles.primaryButtonText}>Save task</Text></Pressable></View></ScrollView>; }
function DetailsScreen({ task, onBack, onEdit, onComplete, onDelete }: any) { return <ScrollView testID="task-details-screen" contentContainerStyle={styles.content}><Pressable testID="details-back-button" onPress={onBack}><Text style={styles.backText}>‹ Back to tasks</Text></Pressable><Text style={styles.eyebrow}>TASK DETAILS</Text><Text style={styles.heading}>{task.title}</Text><View style={styles.detailTags}><Text style={[styles.priority, priorityStyle[task.priority as Priority]]}>{task.priority} priority</Text><Text testID="task-detail-status" style={styles.detailStatus}>{task.status}</Text></View><Detail label="Description" value={task.description || 'No description provided.'} /><Detail label="Due date" value={task.dueDate || 'No due date set'} /><View style={styles.detailActions}>{task.status !== 'Completed' && <Pressable testID="complete-task-button" accessibilityLabel="complete-task-button" style={styles.primaryButtonSmall} onPress={onComplete}><Text style={styles.primaryButtonText}>Mark complete</Text></Pressable>}<Pressable testID="edit-task-button" accessibilityLabel="edit-task-button" style={styles.secondaryButton} onPress={onEdit}><Text style={styles.secondaryButtonText}>Edit</Text></Pressable><Pressable testID="delete-task-button" accessibilityLabel="delete-task-button" style={styles.dangerButton} onPress={onDelete}><Text style={styles.dangerText}>Delete</Text></Pressable></View></ScrollView>; }
function Detail({ label, value }: { label: string; value: string }) { return <View style={styles.detail}><Text style={styles.detailLabel}>{label}</Text><Text style={styles.detailValue}>{value}</Text></View>; }
function SettingsScreen({ darkMode, setDarkMode, onBack, onLogout }: any) { return <ScrollView testID="settings-screen" contentContainerStyle={styles.content}><Pressable testID="settings-back-button" onPress={onBack}><Text style={styles.backText}>‹ Back to dashboard</Text></Pressable><Text style={styles.heading}>Settings</Text><Text style={styles.subtitle}>Manage your workspace preferences.</Text><Text style={styles.settingsLabel}>Account</Text><View style={styles.account}><View style={styles.avatar}><Text style={styles.avatarText}>QA</Text></View><View><Text style={styles.accountName}>QA Engineer</Text><Text style={styles.accountEmail}>qa@example.com</Text></View></View><Text style={styles.settingsLabel}>Appearance</Text><View style={styles.settingRow}><View><Text style={styles.settingTitle}>Dark mode</Text><Text style={styles.settingDescription}>Use a darker workspace theme</Text></View><Switch testID="dark-mode-toggle" accessibilityLabel="dark-mode-toggle" value={darkMode} onValueChange={setDarkMode} trackColor={{ false: '#D6D0C6', true: '#2F6B5D' }} /></View><Pressable testID="settings-logout-button" style={styles.logoutButton} onPress={onLogout}><Text style={styles.logoutText}>Log out</Text></Pressable></ScrollView>; }

const priorityStyle = StyleSheet.create({ Low: { color: '#487A68', backgroundColor: '#E4F1E8' }, Medium: { color: '#9A681C', backgroundColor: '#FFF0D2' }, High: { color: '#A3493E', backgroundColor: '#FBE4E0' } });
const styles = StyleSheet.create({ safeArea: { flex: 1, backgroundColor: '#F8F5EF' }, center: { flex: 1, justifyContent: 'center', padding: 24 }, loginCard: { backgroundColor: '#FFFFFF', borderRadius: 20, padding: 26, shadowColor: '#211D18', shadowOpacity: 0.08, shadowRadius: 18, shadowOffset: { width: 0, height: 8 }, elevation: 3 }, content: { padding: 24, paddingBottom: 48 }, eyebrow: { color: '#2F6B5D', fontSize: 12, fontWeight: '800', letterSpacing: 1.5, marginBottom: 8 }, logo: { color: '#211D18', fontSize: 32, fontWeight: '800', marginBottom: 8 }, heading: { color: '#211D18', fontSize: 28, fontWeight: '800', marginBottom: 6 }, subtitle: { color: '#756F66', fontSize: 15, lineHeight: 22, marginBottom: 24 }, label: { color: '#514B43', fontSize: 13, fontWeight: '700', marginBottom: 8, marginTop: 16 }, input: { backgroundColor: '#FFFFFF', borderColor: '#DED8CE', borderRadius: 10, borderWidth: 1, color: '#211D18', fontSize: 16, minHeight: 50, paddingHorizontal: 14 }, textArea: { minHeight: 100, paddingTop: 14, textAlignVertical: 'top' }, rememberRow: { alignItems: 'center', flexDirection: 'row', marginTop: 16 }, checkbox: { alignItems: 'center', borderColor: '#B8B0A5', borderRadius: 5, borderWidth: 1, height: 20, justifyContent: 'center', marginRight: 9, width: 20 }, checkboxChecked: { backgroundColor: '#2F6B5D', borderColor: '#2F6B5D' }, checkmark: { color: '#FFFFFF', fontWeight: '800' }, body: { color: '#514B43', fontSize: 14 }, error: { color: '#B7473B', fontSize: 13, fontWeight: '600', marginTop: 12 }, primaryButton: { alignItems: 'center', backgroundColor: '#2F6B5D', borderRadius: 10, marginTop: 20, minHeight: 52, justifyContent: 'center' }, primaryButtonSmall: { alignItems: 'center', backgroundColor: '#2F6B5D', borderRadius: 10, minHeight: 48, justifyContent: 'center', paddingHorizontal: 18 }, primaryButtonText: { color: '#FFFFFF', fontSize: 15, fontWeight: '800' }, demoHint: { color: '#756F66', fontSize: 12, marginTop: 20, textAlign: 'center' }, header: { alignItems: 'flex-start', flexDirection: 'row', justifyContent: 'space-between', marginBottom: 20 }, iconButton: { alignItems: 'center', backgroundColor: '#E8E3D9', borderRadius: 22, height: 44, justifyContent: 'center', width: 44 }, iconText: { color: '#514B43', fontSize: 20 }, statsRow: { flexDirection: 'row', gap: 10, marginBottom: 28 }, stat: { backgroundColor: '#E9E5DC', borderRadius: 14, flex: 1, padding: 14 }, statAccent: { backgroundColor: '#DCEBE2' }, statValue: { color: '#211D18', fontSize: 25, fontWeight: '800' }, statLabel: { color: '#756F66', fontSize: 12, marginTop: 4 }, sectionHeader: { alignItems: 'center', flexDirection: 'row', justifyContent: 'space-between', marginBottom: 14 }, sectionTitle: { color: '#211D18', fontSize: 20, fontWeight: '800' }, addButton: { backgroundColor: '#2F6B5D', borderRadius: 9, paddingHorizontal: 12, paddingVertical: 10 }, addButtonText: { color: '#FFFFFF', fontSize: 13, fontWeight: '800' }, search: { backgroundColor: '#FFFFFF', borderColor: '#DED8CE', borderRadius: 10, borderWidth: 1, color: '#211D18', fontSize: 15, height: 48, marginBottom: 12, paddingHorizontal: 14 }, filters: { gap: 8, paddingBottom: 16 }, filterChip: { borderColor: '#D8D1C5', borderRadius: 18, borderWidth: 1, paddingHorizontal: 13, paddingVertical: 8 }, filterActive: { backgroundColor: '#211D18', borderColor: '#211D18' }, filterText: { color: '#756F66', fontSize: 13, fontWeight: '700' }, filterTextActive: { color: '#FFFFFF' }, taskRow: { backgroundColor: '#FFFFFF', borderColor: '#E5DED3', borderRadius: 12, borderWidth: 1, marginBottom: 10, padding: 15 }, taskRowTop: { alignItems: 'center', flexDirection: 'row', justifyContent: 'space-between' }, taskTitle: { color: '#211D18', flex: 1, fontSize: 16, fontWeight: '800', marginRight: 10 }, priority: { borderRadius: 6, fontSize: 11, fontWeight: '800', overflow: 'hidden', paddingHorizontal: 8, paddingVertical: 5 }, taskRowBottom: { flexDirection: 'row', justifyContent: 'space-between', marginTop: 13 }, statusText: { color: '#2F6B5D', fontSize: 12, fontWeight: '700' }, dueText: { color: '#8B847A', fontSize: 12 }, empty: { color: '#756F66', fontSize: 15, paddingVertical: 25, textAlign: 'center' }, logoutButton: { alignItems: 'center', borderColor: '#C86A5B', borderRadius: 10, borderWidth: 1, marginTop: 28, minHeight: 48, justifyContent: 'center' }, logoutText: { color: '#B7473B', fontSize: 15, fontWeight: '800' }, formHeader: { marginBottom: 12 }, backText: { color: '#2F6B5D', fontSize: 15, fontWeight: '800', marginBottom: 24 }, optional: { color: '#9A9389', fontWeight: '400' }, optionRow: { flexDirection: 'row', gap: 8 }, option: { alignItems: 'center', borderColor: '#D8D1C5', borderRadius: 9, borderWidth: 1, flex: 1, paddingVertical: 13 }, optionSelected: { backgroundColor: '#DCEBE2', borderColor: '#2F6B5D' }, optionText: { color: '#756F66', fontSize: 13, fontWeight: '700' }, optionTextSelected: { color: '#2F6B5D', fontSize: 13, fontWeight: '800' }, buttonRow: { flexDirection: 'row', gap: 10, marginTop: 28 }, secondaryButton: { alignItems: 'center', borderColor: '#B8B0A5', borderRadius: 10, borderWidth: 1, flex: 1, justifyContent: 'center', minHeight: 48 }, secondaryButtonText: { color: '#514B43', fontSize: 15, fontWeight: '800' }, detailTags: { alignItems: 'center', flexDirection: 'row', gap: 8, marginBottom: 24 }, detailStatus: { backgroundColor: '#E9E5DC', borderRadius: 6, color: '#514B43', fontSize: 11, fontWeight: '800', overflow: 'hidden', paddingHorizontal: 8, paddingVertical: 5 }, detail: { borderBottomColor: '#E2DCD2', borderBottomWidth: 1, paddingVertical: 18 }, detailLabel: { color: '#8B847A', fontSize: 12, fontWeight: '800', textTransform: 'uppercase' }, detailValue: { color: '#211D18', fontSize: 16, lineHeight: 23, marginTop: 7 }, detailActions: { gap: 10, marginTop: 26 }, dangerButton: { alignItems: 'center', borderColor: '#C86A5B', borderRadius: 10, borderWidth: 1, justifyContent: 'center', minHeight: 48 }, dangerText: { color: '#B7473B', fontSize: 15, fontWeight: '800' }, settingsLabel: { color: '#8B847A', fontSize: 12, fontWeight: '800', marginTop: 30, textTransform: 'uppercase' }, account: { alignItems: 'center', backgroundColor: '#FFFFFF', borderColor: '#E5DED3', borderRadius: 12, borderWidth: 1, flexDirection: 'row', marginTop: 12, padding: 16 }, avatar: { alignItems: 'center', backgroundColor: '#DCEBE2', borderRadius: 24, height: 48, justifyContent: 'center', marginRight: 12, width: 48 }, avatarText: { color: '#2F6B5D', fontWeight: '800' }, accountName: { color: '#211D18', fontSize: 16, fontWeight: '800' }, accountEmail: { color: '#756F66', fontSize: 13, marginTop: 4 }, settingRow: { alignItems: 'center', backgroundColor: '#FFFFFF', borderColor: '#E5DED3', borderRadius: 12, borderWidth: 1, flexDirection: 'row', justifyContent: 'space-between', marginTop: 12, padding: 16 }, settingTitle: { color: '#211D18', fontSize: 16, fontWeight: '800' }, settingDescription: { color: '#756F66', fontSize: 13, marginTop: 4 } });
